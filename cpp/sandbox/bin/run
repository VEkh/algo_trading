#!/bin/bash

srcs="${@:-./sandbox.cpp}"

g++ \
  -std=c++17 \
  -iquote. \
  -iquote./src \
  -I./deps/libwebsockets/build/include \
  -L./deps/libwebsockets/build/lib \
  $args \
  -lwebsockets \
  -o ./.output &&
  LD_LIBRARY_PATH=./deps/libwebsockets/build/lib ./.output;

\rm -f ./.output
